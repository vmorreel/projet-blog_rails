%div
    - if current_user
        <br>
        =link_to"Create post", [:new, :post], {:class => "btn"}
    %h2 Posts list :
    %ul
        -Post.all.each do |post|
            %section{:class => "posts"}
                %div
                    =link_to "#{post.title}", [post]
                    %span= "#{post.created_at}"
                %p= "#{post.lead_paragraph}"


